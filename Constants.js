const Constants = {
  GRID_SIZE: 25,  // Defines the number of cells in the grid (width and height)
  CELL_SIZE: 20,  // Defines the size of each cell in pixels
  MAX_WIDTH: 500, // Maximum width of the game area (adjust according to screen size)
  MAX_HEIGHT: 500 // Maximum height of the game area (adjust according to screen size)
};

export default Constants;
